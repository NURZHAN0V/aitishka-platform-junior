# Экраны и навигация

## Навигация

| Группа | Раздел | Макет |
| --- | --- | --- |
| — | Главная | [glavnaya.jpg](../design/references/glavnaya.jpg) |
| Учёба | Расписание | [raspisanie.jpg](../design/references/raspisanie.jpg) |
| Учёба | Оценки | реализовано (`/grades`) |
| Учёба | Домашние задания | реализовано (`/homework`) |
| Учёба | Экзамены | реализовано (`/exams`) |
| Учёба | Рейтинг | реализовано (`/rating`) |
| Активность | Журнал | реализовано (`/journal`) |
| Активность | Монетки | реализовано (`/coins`) |
| Магазин | Маркет | реализовано (`/market`) · [market.jpg](../design/references/market.jpg) |
| Магазин | Мои заказы | реализовано (`/orders`) |
| Финансы | Платежи | реализовано (`/payments`) |
| — | Меню профиля (шапка) | [profil-modal.jpg](../design/references/profil-modal.jpg) · профиль (`/profile`), отзывы (`/reviews`), настройки (`/settings`), помощь (`/help`) |

## Главная

**Виджеты:**

1. Приветствие + баннер
2. Мой рейтинг — место в группе, балл, топ-5
3. Домашние задания — счётчики «на проверке» и «просроченные»
4. Серии и монетки — прогресс без пропусков / без опозданий, подсказка про фото
5. Оценки и посещаемость — мини-график за текущую неделю, средняя посещаемость
6. Журнал — последние события (7 дней)
7. Быстрые ссылки — расписание, экзамены, маркет, рейтинг

## Расписание

**Переключатели:** День | Неделя | Месяц. Кнопка «Сегодня» возвращает к текущему периоду.

| Вид | Поведение |
| --- | --- |
| День | Занятия выбранного дня: сначала «идёт сейчас», затем ближайшие по времени. Для сегодня — блок «Дальше на этой неделе» (до 3 занятий). Если день пуст — подсказка и ближайшее следующее |
| Неделя | 7 колонок Пн–Вс на широком экране; на узком — список дней. Текущий день с кольцом и бейджем «Сегодня» |
| Месяц | Сетка текущего месяца с точками предметов; клик по дню открывает вид «День» |

**Карточка занятия:** предмет, время, статус. Клик раскрывает детали (преподаватель, аудитория / онлайн). Тему задаёт преподаватель в начале занятия — в расписании её нет.

**Статусы:** Скоро · Идёт сейчас · Прошло. Прошедшие — приглушены, без кнопки перехода.

Прошедшие занятия — без действий. Отменённые — не показываются.

## Оценки

Маршрут: `/grades`. Макет отдельный отсутствует — визуально как расписание и главная (токены, `AppLayout`, карточки предметов).

**Быстрый доступ (верхняя полоса):**

1. Средний балл за выбранный период (шкала 2–5) + качественная подпись
2. Динамика успеваемости — мини-график (sparkline), без тяжёлой chart-библиотеки
3. Последние оценки с иконкой предмета и маркерами статуса

**Мотивация:** вклад оценок в рейтинг (50% средний балл + 50% ДЗ вовремя) и монетки за пятёрки по ДЗ (+2, если сдано вовремя и зачтено).

**Панель:** период День | Неделя | Месяц (по умолчанию текущий календарный месяц), фильтры предмета и типа работы, переключатель Сводный | Детализированный.

**Список:**

| Вид | Поведение |
| --- | --- |
| Сводный | Карточки предметов: средний балл, число оценок, маркеры последней |
| Детализированный | Карточки оценок; клик раскрывает детали на месте (преподаватель, тип, комментарий, монетки) |

**Маркеры (BaseChip):** Отлично · Экзамен · Пересдача · Не аттестован · С опозданием.

Пустой период и «пусто по фильтрам» — в стиле empty-карточек расписания.

## Домашние задания

Маршрут: `/homework`. Макет: [domashnie-zadaniya.jpg](../design/references/domashnie-zadaniya.jpg).

**Быстрый доступ:** счётчики «на проверке» и «просроченные» (клик открывает вкладку).

**Панель:** фильтр предмета, без выбора периода.

**Фильтры-вкладки:** Текущие · На проверке · Проверенные · Отклонённые · Просроченные (с числом в каждой). Несданная работа до срока — «Текущие»; после срока — «Просроченные»; отклонённая и ждущая перезагрузку — «Отклонённые».

**Список:** сначала 8 заданий, кнопка «Показать ещё» подгружает по 4.

**Карточка:** обложка предмета, статус, название, тема, преподаватель, срок (цвет: зелёный / жёлтый / красный), кнопки «Скачать» и «Загрузить». Клик раскрывает описание, материалы и комментарий преподавателя.

**Модал загрузки:** drag-and-drop или выбор файла → «Отправить». Успех — тост «Работа отправлена на проверку». На проверке можно удалить файл и загрузить заново.

## Экзамены

Маршрут: `/exams`. Макет отдельный отсутствует — визуально как домашние задания и оценки.

**Быстрый доступ:** счётчики «ожидают загрузки», «ближайшие сроки» и «пропущено» (клик открывает вкладку). Полоса ближайших экзаменов с маркерами Срочно / Скоро.

**Мотивация:** оценка 2–5 входит в средний балл; рейтинг — 50% средний балл + 50% ДЗ вовремя. За экзамен монетки не начисляются.

**Панель:** список за весь период обучения (без переключения месяца); фильтр предмета.

**Фильтры-вкладки:** Все · Ожидает загрузки · Загружено · Проверено · Пропущено.

**Карточка:** предмет, преподаватель, оценка, конечная дата, дата проверки, статус. Клик раскрывает программу, материалы и рекомендации. «Загрузить» — один раз, кнопка до даты проверки; после оценки файл можно открыть.

## Рейтинг

Маршрут: `/rating`. Макет отдельный отсутствует — визуально как виджет «Мой рейтинг» на главной ([glavnaya.jpg](../design/references/glavnaya.jpg)).

**Период:** всё время обучения (без переключения на месяц). Пересчёт каждый день.

**Формула:** 50% средний балл за всё время (шкала 2–5) + 50% доля ДЗ, сданных вовремя и зачтённых.

**Слева:** место в группе («N из M»), баллы, подпись периода («За всё время»), два прогресс-бара — средний балл и ДЗ вовремя.

**Справа:** топ лидеров группы, строка текущего пользователя выделена. «Вся таблица» раскрывает полный список на этой же странице (без отдельного маршрута).

**Мобильные:** компактная сводка; прогресс-бары и лидеры раскрываются по кнопке «Показать детали».

**Мотивация:** монетки в рейтинг не входят; средний балл и ДЗ вовремя влияют и на место, и на начисления.

## Журнал

Маршрут: `/journal`. Макет отдельный отсутствует — визуально как остальные разделы (токены, `AppLayout`, лента карточек).

**Виджет на главной:** события за последние 7 дней, ссылка «Весь журнал» → `/journal`.

**Панель:** чипы-фильтры «Все» и по типам событий (с числом в каждом).

**Типы:** посещение · сдача ДЗ · сдача экзамена · оценка · монетки · отзыв преподавателя · опрос · заказ в маркете · отзыв на Яндекс на проверке.

**Лента:** сначала 12 событий, кнопка «Показать ещё» подгружает по 12. Карточка: иконка, что случилось, контекст (предмет / задание), дата. Клик по связанным типам ведёт в раздел (ДЗ, оценки, экзамены, расписание, маркет, отзывы).

Push-уведомлений нет — журнал основной способ узнавать, что произошло.

## Отзывы

Маршрут: `/reviews`. Макет отдельный отсутствует — визуально как остальные разделы (токены, `AppLayout`, карточки).

**Только чтение** — отзыв создаёт и правит преподаватель в своём кабинете; удалить нельзя. Студент и родитель видят одно и то же (один аккаунт).

**Панель:** краткая подсказка, счётчик, фильтр предмета (Все / Python / Scratch / …).

**Список:** новые сверху. Карточка: преподаватель, предмет, дата, полный текст. Без кнопок создания, правки и удаления.

**Связи:** меню профиля в шапке → «Мои отзывы»; события журнала «Получил отзыв от преподавателя» → `/reviews`.

## Монетки

Маршрут: `/coins`. Макет отдельный отсутствует — визуально как остальные разделы и виджет «Серии и монетки» на главной ([glavnaya.jpg](../design/references/glavnaya.jpg)).

**Баланс:** крупное число (согласовано с шапкой), кнопка «Потратить в маркете» → `/market`.

**Серии:** два независимых прогресса — «N из M без пропусков» и «N из M без опозданий» (те же данные, что на виджете главной). Рядом — награда за следующую ступень.

**Шпаргалка:** раскрываемый блок «Как получить монетки» по таблице начислений (коротко, без стены текста).

**История:** лента начислений и списаний («за что пришло / куда ушло»), фильтры Все · Пришло · Ушло, «Показать ещё».

**Связи:** сайдбар «Активность» → Монетки; виджет на главной «Мои монетки →»; события монеток в журнале ведут сюда. Баланс в шапке общий с маркетом (после покупки обновляется).

## Маркет

Маршрут: `/market`. Макет: [market.jpg](../design/references/market.jpg).

**Каталог:** сетка товаров, фильтр категорий (Все / Мерч / Канцелярия / Игрушки), сортировка (дешёвые / дорогие / по названию).

**Баланс:** на странице маркета и в шапке — один и тот же счётчик монеток (обновляется после покупки).

**Карточка товара:** фото, название, цена в монетках, кнопка «Купить» / «Не хватает монеток» / «Нет в наличии».

**Покупка:** модал с балансом после покупки и пометкой «Забрать в школе на занятиях» → списание монеток → заказ со статусом «Оформлен». Студент заказ не отменяет.

**Связи:** сайдбар «Магазин» → Маркет; кнопка «Мои заказы»; CTA с `/coins` «Потратить в маркете».

## Мои заказы

Маршрут: `/orders`.

**Список:** товар, цена, дата, статус. Статусы строго: **Оформлен → Готов к выдаче → Получен**. Кнопка отмены нет.

**Связи:** сайдбар «Магазин» → Мои заказы; с маркета — «Мои заказы».

## Платежи

Маршрут: `/payments`. Макет отдельный отсутствует — визуально как остальные разделы (токены, `AppLayout`, карточки).

**Только просмотр** — данные вносит учебная часть. Одна страница для ребёнка и родителя.

**Панель:** период Месяц (по умолчанию текущий) | Весь период; для месяца — навигация стрелками и «Текущий месяц».

**Блоки:**

1. **Реквизиты** — получатель, ИНН, БИК, расчётный счёт, банк, код студента, назначение платежа с персональным кодом. Кнопка «Скопировать» у каждого поля и «Скопировать всё».
2. **График платежей** — таблица «Оплатить до | Описание | К оплате» (фильтр по месяцу даты). **Задолженность** = сумма по наступившим строкам графика − сумма всех оплат; красная строка с минусом — только если задолженность > 0.
3. **История платежей** — таблица «Дата | Назначение платежа | Оплачено» (фильтр по месяцу даты оплаты).

**Связи:** сайдбар «Финансы» → Платежи.

## Личный кабинет (мой профиль)

Маршрут: `/profile`. Макет меню: [profil-modal.jpg](../design/references/profil-modal.jpg). Отдельного макета экрана нет — визуально как остальные разделы.

**Композиция:** сетка как у настроек — слева основной блок (аватар + данные), справа колонка **Подарок**.

**Данные:** ФИО, группа, школа, телефон, почта, дата рождения — все только просмотр. В шапке блока: аватар и имя; лёгкая подсказка со ссылкой в настройки. Без чипа «Отклонено» и без причины отклонения (они только в настройках). Филиал — Сочи; города в профиле нет.

**Редактирование:** на этом экране ничего не меняется. **Фото и аватарку** — только в «Настройках аккаунта». Остальное задаёт учебная часть.

Один текущий аватар в шапке, профиле и настройках (общий session state).

**Подарок:** правая колонка — polaroid («IT CAMP» / «Гимназия №16») и призыв загрузить **своё** фото: **+5 после одобрения**. Без чеклиста и процента. Состояния: нет подарка → CTA в `/settings`; на проверке — «ждём одобрения»; одобрено — «подарок получен» и ссылка в монетки. Clay-аватарка подарок не даёт. Бонус **+5 монеток разово** только за одобренное загруженное фото (как в разделе «Монетки»).

**Не дублируем** отзывы (они на `/reviews`).

**Связи:** меню профиля в шапке → «Мой профиль»; виджет «Загрузи фото» на главной → `/profile`.

## Настройки аккаунта

Маршрут: `/settings`. Макет меню: [profil-modal.jpg](../design/references/profil-modal.jpg). Отдельного макета экрана нет — визуально как остальные разделы.

**Фокус:** **фото/аватарка** (единственное место смены) и привязка Яндекс / ВКонтакте (мок: кнопка «Привязать …» ↔ «Отвязать …», без реального OAuth). Не дублирует весь личный кабинет — краткая подсказка и ссылка на `/profile`.

**Фото и аватар:**

1. **Загрузка файла** — статусы нет фото / на проверке / одобрено / отклонено (с причиной). Пока на проверке — повторная загрузка недоступна. Модерация учебной части (mock на клиенте).
2. **Генерация аватарки** — DiceBear **Clay** (локально через `@dicebear/core` + `@dicebear/styles`, seed → SVG data URL, без HTTP API). Сразу становится текущим фото, **без модерации**. Модалка: один превью + «Выбрать» / «Сгенерировать».

**Связи:** меню профиля в шапке → «Настройки аккаунта».

## Помощь и поддержка

Маршрут: `/help`. Макет отдельный отсутствует — визуально как остальные разделы (токены, `AppLayout`, карточки).

**Контакты школы:** ООО «ИТ СОЧИ» / IT Camp Sochi, программа «IT-старт», город Сочи; телефон `tel:+79531111057`, почта `mailto:c4mp.it@yandex.ru`, сайт [itcampsochi.ru](https://itcampsochi.ru/) (внешняя ссылка).

**FAQ (коротко):** связь с учебной частью; где расписание и ДЗ (`/schedule`, `/homework`); вход и смена пароля — только через учебную часть; отчисление — доступ закрыт; один аккаунт у ребёнка и родителя; где платежи (`/payments`).

**Не дублируем:** отзывы (`/reviews`), профиль (`/profile`). Без форм смены пароля и админских действий.

**Связи:** меню профиля в шапке → «Помощь и поддержка».

## Меню профиля (шапка)

Открывается по клику на блок с аватаром в шапке. Белая карточка со скруглением, тень.

**Шапка меню:** аватар, ФИО, группа.

**Пункты в MVP:**

| Пункт в макете | Куда ведёт |
| --- | --- |
| Мой профиль | Личный кабинет (`/profile`) — просмотр; виджет «Подарок» справа |
| Мои отзывы | Отзывы (`/reviews`) |
| Настройки аккаунта | Настройки (`/settings`) — фото (модерация), привязка Яндекс/ВКонтакте |
| Помощь и поддержка | Страница помощи (`/help`) — контакты школы и FAQ |
| Выйти из аккаунта | Выход из дневника |

**Не входит в MVP** (на макете есть, в продукте не делаем):

- Уведомления — push нет, студент смотрит «Журнал»
- Безопасность — смена пароля только через учебную часть

Привязка Яндекс / ВКонтакте — на экране «Настройки аккаунта» (мок bind/unbind), не отдельный пункт меню.

## Вход (без макета)

- Поля: логин, пароль
- «Забыли пароль» → восстановление по почте или форма заявки
- Отчислённый студент — экран с просьбой обратиться в учебную часть
